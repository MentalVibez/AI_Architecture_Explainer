# ── Add to existing backend Dockerfile ──────────────────────────────────────
# After the pip install step, add:

RUN pip install ruff bandit pip-audit

# For gitleaks (if network allows):
# RUN curl -sSL https://github.com/gitleaks/gitleaks/releases/download/v8.21.2/gitleaks_8.21.2_linux_x64.tar.gz \
#     | tar -xz -C /usr/local/bin gitleaks

# git is required for the clone step (Option A)
# On Railway, git is usually pre-installed. If not:
# RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*

# Verify adapters are installed:
# RUN ruff --version && bandit --version && pip-audit --version
